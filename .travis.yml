dist: bionic
sudo: required

install:
  - bash -ex .travis-ocaml.sh
  - eval $(opam env); sh setup-opam

script:
  - eval $(opam env); dune runtest

env:
  matrix:
  - OCAML_VERSION=4.06
  - OCAML_VERSION=4.07

os:
  - linux
  - mac
