dist: bionic
sudo: required

install:
  - cat /etc/lsb-release
  - bash -ex .travis-ocaml.sh
  - eval $(opam env); sh setup-opam

script:
  - eval $(opam env); make
  - opam runtest

env:
  matrix:
  - OCAML_VERSION=4.06
  - OCAML_VERSION=4.07

os:
  - linux
  - mac
